#include "main.h"

/**
 * _abs - Check
 * @i: An input character
 * Description: function uses _abs
 * integral o
 * Return: 0
 */

int _abs(int i)
{
	if (i >= 0)
		return (n);
	else
		return (-n);
}
