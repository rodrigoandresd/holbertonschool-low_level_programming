#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char c;
int i;
{
for (i = '0' ; i <= '9' ; i++)
	putchar(i);
for (c = 'a' ; c <= 'z' ; c++)
putchar(c);
}
putchar('\n');
return (0);
}
